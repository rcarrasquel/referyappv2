<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect to root index.php which will handle the routing
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>
